# Frontend Configuration for MoodSync
# Renombra este archivo a .env y configura las variables

# Backend API URL (en desarrollo, usar el puerto del backend)
VITE_API_BASE=http://localhost:4000

# Production API URL (comentado por defecto)
# VITE_API_BASE=https://tu-dominio-vercel.vercel.app

# Nota: En producción con Vercel, VITE_API_BASE puede quedar vacío 
# ya que frontend y backend están en el mismo dominio
